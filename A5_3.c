#include <stdio.h>

int main
{
    int nos;
    scanf("%d", &n);
    int count[n];
    for (i = 0; i < n; i++) {
      count[i] = 0;
    }
    for (i = 0; i < n; i++) {
        
    }

    return 0;
}
